import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    answersContainer: {
        position: 'absolute',
        width: '100%',
        marginTop: 440,
    }
})

export default styles;