import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    modeListContainer: {
        width: '100%',
        height: '100%',
        alignItems: 'center',
    }

})

export default styles;