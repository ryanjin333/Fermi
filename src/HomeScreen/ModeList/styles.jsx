import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    modeListContainer: {
        width: '100%',
        height: '100%',
        alignItems: 'center',
        flex: 1,
    }

})

export default styles;